alert('LES  OPERATEURS');


// L'addition

//1 Déclaration de plusieurs variables :

var nb1, nb2, resultat;

nb1 = 10; nb2 = 5;

// L'addition de nb1 & nb2 avec l'opérateur  " + "
resultat = nb1 + nb2;
console.log(resultat);

// La soustraction de nb1 & nb2 avec l'opérateur " - "
resultat = nb1 - nb2;
console.log(resultat);

// La division de nb1 & nb2 avec l'opérateur " / "
resultat = nb1 / nb2;
console.log(resultat);

// La multiplication de nb1 & nb2 avec l'opérateur
resultat = nb1 * nb2;
console.log(resultat);

/* Le modulo correspond au reste de la division.
Le modulo de nb1 & nb 2 avec l'opérateur " % "
*/
resultat = nb1 % nb2;
console.log(resultat);

/************************* 
LES ECRITURES SIMPLIFIEES
*************************/

// Addition

// Je créée ma variable nb1 et je lui affecte une valeur de 15 (avec le symbole =)
nb1 = 15;
//  premiere variante :
// Pour ajouter 5 à ma valeur de 15,je réaffecte à ma variable une nouvelle instruction :
// je lui dit que ma variable nb1 sera maintenant le resultat de ma valeur de base le résultat de ma valeur de base (donc 15) auquel le lui rajoute 5 : 
// /* ma variable La valeur de reference *

// nb1 = nb1 + 5; // ici on dit que le resultat final de nbl est la somme de notre variable (nb1) +5 donc 20

// console.log(nb1); //   retourne 20 donc 15 + 5 
console.log(typeof nb1);

// Deuxième variante
nb1 += 5; // Ce qui est équivalent à écrire "nb1 = nb1 + 5"
console.log(nb1);

/*
    Je peux proceder de la meme manière que les oprérateurs arithmétiques:
    " + ", " - ", " * ", " et ", " % ",
*/
